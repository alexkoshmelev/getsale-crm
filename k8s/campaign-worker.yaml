apiVersion: apps/v1
kind: Deployment
metadata:
  name: campaign-worker
  namespace: getsale
  labels:
    app.kubernetes.io/name: campaign-worker
    app.kubernetes.io/part-of: getsale
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: campaign-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: campaign-worker
        app.kubernetes.io/part-of: getsale
    spec:
      containers:
        - name: campaign-worker
          image: getsale/campaign-worker:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: getsale-common-config
            - secretRef:
                name: getsale-secrets
          env:
            - name: WORKER_CONCURRENCY
              valueFrom:
                configMapKeyRef:
                  name: getsale-common-config
                  key: WORKER_CONCURRENCY
          ports:
            - containerPort: 4016
              protocol: TCP
              name: health
          readinessProbe:
            httpGet:
              path: /health
              port: 4016
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 4016
            initialDelaySeconds: 15
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
