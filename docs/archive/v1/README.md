# Archived v1 references

Documents elsewhere under `docs/` may still mention removed paths (`services/*`, `shared/*`, old `docker-compose.yml`). They are kept for historical context; treat [`docs/README.md`](../../README.md) and the v2 tree in the repository as authoritative.
